@model iLgs.Models.ChangePasswordViewModel
@{
    ViewBag.Title = "Reset Password";
}

<h2 class="text-info">@ViewBag.Title.</h2>

@using (Html.BeginForm("ResetPassword", "Manage", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{

    @Html.AntiForgeryToken()
    @Html.Hidden("userId")
    <hr />
    @Html.ValidationSummary("", new { @class = "text-danger" })

    <div class="form-group">
        <div class="col-md-5">
            @(Html.Kendo().TextBox()
                    .Name("userName").Enable(false)
                    .Label(l => l.Content("User Name").Floating(true))
            )
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-5">
            @(Html.Kendo().TextBox()
                    .Name("nameFull").Enable(false)
                    .Label(l => l.Content("Full Name").Floating(true))
            )

        </div>
    </div>
    <div class="form-group">
        <div class="col-md-5">
            @(Html.Kendo().TextBoxFor(m => m.NewPassword)
                    .HtmlAttributes(new { type = "password" })
                    .Label(l => l.Content("New Password").Floating(true))
            )
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-5">
            @(Html.Kendo().TextBoxFor(m => m.ConfirmPassword)
                    .HtmlAttributes(new { type = "password" })
                    .Label(l => l.Content("Confirm Password").Floating(true))
            )
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-5">
            <input type="submit" value="Reset password" class="btn btn-default" />
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

<style>
    .k-floating-label-container {
        width: 100%;
    }
</style>