@model iLgs.Models.GenerateIcsParVM

@using (Html.BeginForm(null, null, FormMethod.Post, new { name = "form", id = "form" }))
{
    @Html.AntiForgeryToken()

    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    <div id="ErrorContent" class="hidden x-bootstrap-content">
        <div class="alert alert-danger x-popup-alert">
            <ul class="errors"></ul>
        </div>
    </div>

    //@Html.HiddenFor(model => model.Id)
    @Html.HiddenFor(model => model.PsCardItemId)
    @Html.HiddenFor(model => model.RefType)
    @Html.HiddenFor(model => model.Location)
    @Html.HiddenFor(model => model.SelectedIds)

    <fieldset>
        <div class="row">
            <div class="col-md-12">
                @Html.LabelFor(model => model.Date, "Reference Date", htmlAttributes: new { @class = "col-md-2" })
                <div class="col-md-10">
                    @(Html.Kendo().DatePickerFor(model => model.Date).Enable(true).HtmlAttributes(new { style = "width:100%; max-width:100%;" }))
                    @Html.ValidationMessageFor(model => model.Date)
                </div>
            </div>
        </div>

        @*<div class="row">
            <div class="col-md-12">
                @Html.LabelFor(model => model.Qty, htmlAttributes: new { @class = "col-md-2" })
                <div class="col-md-10">
                    @(Html.Kendo().NumericTextBoxFor(model => model.Qty).Decimals(2).Format("{0:n0}").HtmlAttributes(new { style = "width:100%; max-width:100%;" }))
                    @Html.ValidationMessageFor(model => model.Qty)
                </div>
            </div>
        </div>*@

        <div class="row">
            <div class="col-md-12">
                @Html.LabelFor(model => model.LocationId, htmlAttributes: new { @class = "col-md-2" })
                <div class="col-md-10">
                    @(Html.Kendo().MultiColumnComboBoxFor(m => m.LocationId)
                            .HtmlAttributes(new { style = "width:100%; max-width:100%;" })
                            .Placeholder("Select Location")
                            .DataTextField("Description")
                            .DataValueField("Id")
                            .ValuePrimitive(true)
                            .Filter(FilterType.Contains)
                            .DataSource(source =>
                            {
                                source.Read(read =>
                                {
                                    read.Action("GetCodeList", "Getters", new { mastCode = "LOCATIONS", addAll = false });
                                })
                                .ServerFiltering(true);
                            })
                            .Columns(c =>
                            {
                                c.Add().Field("Code").Title("Code").Width("30%");
                                c.Add().Field("Description").Title("Description").Width("70%");
                            })
                            .Events(e => e.Select("onSelectGenerateParLocationId"))
                            .Suggest(true)
                    )
                    @Html.ValidationMessageFor(model => model.LocationId)
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-md-12">
                @Html.LabelFor(model => model.LocationCode, htmlAttributes: new { @class = "col-md-2" })
                <div class="col-md-10">
                    @(Html.Kendo().TextBoxFor(model => model.LocationCode).Enable(false).HtmlAttributes(new { style = "width:100%; max-width:100%;" }))
                    @Html.ValidationMessageFor(model => model.LocationCode)
                </div>
            </div>
        </div>

        <br />
        <div class="row">
            <div class="col-md-12">
                @Html.LabelFor(m => m.IcsPar.ReceivedBy, htmlAttributes: new { @class = "col-md-2" })
                <div class="col-md-10">
                    @(Html.Kendo().TextBoxFor(m => m.IcsPar.ReceivedBy).HtmlAttributes(new { style = "width:100%; max-width:100%;" }))
                    @Html.ValidationMessageFor(m => m.IcsPar.ReceivedBy)
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-md-12">
                @Html.LabelFor(m => m.IcsPar.ReceivedByPosition, htmlAttributes: new { @class = "col-md-2" })
                <div class="col-md-10">
                    @(Html.Kendo().TextBoxFor(m => m.IcsPar.ReceivedByPosition).HtmlAttributes(new { style = "width:100%; max-width:100%;" }))
                    @Html.ValidationMessageFor(m => m.IcsPar.ReceivedByPosition)
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-md-12">
                @Html.LabelFor(m => m.IcsPar.ReceivedDept, htmlAttributes: new { @class = "col-md-2" })
                <div class="col-md-10">
                    @(Html.Kendo().TextBoxFor(m => m.IcsPar.ReceivedDept).HtmlAttributes(new { style = "width:100%; max-width:100%;" }))
                    @Html.ValidationMessageFor(m => m.IcsPar.ReceivedDept)
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-md-12">
                @Html.LabelFor(m => m.IcsPar.ReceivedDate, htmlAttributes: new { @class = "col-md-2" })
                <div class="col-md-10">
                    @(Html.Kendo().DatePickerFor(m => m.IcsPar.ReceivedDate).HtmlAttributes(new { style = "width:100%; max-width:100%;" }))
                    @Html.ValidationMessageFor(m => m.IcsPar.ReceivedDate)
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-md-12">
                @Html.LabelFor(m => m.IcsPar.IssuedBy, htmlAttributes: new { @class = "col-md-2" })
                <div class="col-md-10">
                    @(Html.Kendo().TextBoxFor(m => m.IcsPar.IssuedBy).HtmlAttributes(new { style = "width:100%; max-width:100%;" }))
                    @Html.ValidationMessageFor(m => m.IcsPar.IssuedBy)
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-md-12">
                @Html.LabelFor(m => m.IcsPar.IssuedByPosition, htmlAttributes: new { @class = "col-md-2" })
                <div class="col-md-10">
                    @(Html.Kendo().TextBoxFor(m => m.IcsPar.IssuedByPosition).HtmlAttributes(new { style = "width:100%; max-width:100%;" }))
                    @Html.ValidationMessageFor(m => m.IcsPar.IssuedByPosition)
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-md-12">
                @Html.LabelFor(m => m.IcsPar.IssuedDept, htmlAttributes: new { @class = "col-md-2" })
                <div class="col-md-10">
                    @(Html.Kendo().TextBoxFor(m => m.IcsPar.IssuedDept).HtmlAttributes(new { style = "width:100%; max-width:100%;" }))
                    @Html.ValidationMessageFor(m => m.IcsPar.IssuedDept)
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-md-12">
                @Html.LabelFor(m => m.IcsPar.IssuedDate, htmlAttributes: new { @class = "col-md-2" })
                <div class="col-md-10">
                    @(Html.Kendo().DatePickerFor(m => m.IcsPar.IssuedDate).HtmlAttributes(new { style = "width:100%; max-width:100%;" }))
                    @Html.ValidationMessageFor(m => m.IcsPar.IssuedDate)
                </div>
            </div>
        </div>
        <br />

        <div class="row" style="padding-left:20px;padding-right:20px;">
            @RenderGridSelections()
        </div>

        <br />
        <div class="row">
            <div class="col-md-12">
                <span id="span_">
                    @(Html.Kendo().Button().Icon("k-icon k-i-check").Content("Submit").Name("btnSave"))
                    @(Html.Kendo().Button().Icon("k-icon k-i-cancel").Content("Close").Name("btnCancel"))
                </span>
            </div>
        </div>
    </fieldset>
}

@helper RenderGridSelections()
{
if (@ViewBag.ItemExtnName == "ItemExtnOther")
{
        @(Html.Kendo().Grid<iLgs.Models.PsCardItemExtnOther>()
            .Name("gridSelections")
            .Columns(col =>
            {
                col.Select().Width(30);
                col.Bound(c => c.SerialNo).Width(100);
            })
            .Filterable()
            .NoRecords()
            .Resizable(r => r.Columns(true))
            .Selectable(s => s.Mode(GridSelectionMode.Multiple))
            .Sortable()
            .Scrollable(s => s.Height(200))
            .DataSource(dataSource => dataSource
                .Ajax()
                .Events(events => events.Error("GridError"))
                .Model(model =>
                {
                    model.Id(p => p.Id);
                    model.Field(p => p.Id).DefaultValue(Guid.Empty).Editable(true);
                })
                .Sort(s =>
                {
                    s.Add(a => a.SerialNo);
                })
                .Read(read => read.Action("_GenerateIcsSelectionRead", "IcsSet", new { psCardItemId = @ViewData["psCardItemId"] }))
            )
            .AutoBind(true)
        )
}
else if (@ViewBag.ItemExtnName == "ItemExtnVehicle")
{
        @(Html.Kendo().Grid<iLgs.Models.PsCardItemExtnVehicle>()
            .Name("gridSelections")
            .Columns(col =>
            {
                col.Select().Width(30);
                col.Bound(c => c.YearModel).Width(50);
                col.Bound(c => c.PlateNo).Width(50);
                col.Bound(c => c.BodyNo).Width(70);
                col.Bound(c => c.EngineNo).Width(70);
                col.Bound(c => c.ChasisNo).Width(70);
                col.Bound(c => c.Color).Width(50);
                col.Bound(c => c.CRN).Width(70);
                col.Bound(c => c.CRDate).Width(50);
                col.Bound(c => c.MVFileNo).Width(70);
            })
            .Filterable()
            .NoRecords()
            .Resizable(r => r.Columns(true))
            .Selectable(s => s.Mode(GridSelectionMode.Multiple))
            .Sortable()
            .Scrollable(s => s.Height(200))
            .DataSource(dataSource => dataSource
                .Ajax()
                .Events(events => events.Error("GridError"))
                .Model(model =>
                {
                    model.Id(p => p.Id);
                    model.Field(p => p.Id).DefaultValue(Guid.Empty).Editable(true);
                })
                .Sort(s =>
                {
                    s.Add(a => a.PlateNo);
                })
                .Read(read => read.Action("_GenerateIcsSelectionRead", "IcsSet", new { psCardItemId = @ViewData["psCardItemId"] }))
            )
            .AutoBind(true)
        )
}
}


<script type="text/javascript">
    //function onDataBoundGrid(e) {
    //    var grid = this;
    //    var selectedRows = this.select();
    //    var totalSelectedRow = this.selectedKeyNames().length;
    //    if (totalSelectedRow == 0) {
    //        $("#btnSave").html("Generate");
    //    } else {
    //        $("#btnSave").html("Generate " + totalSelectedRow + " record(s)");
    //    }
    //    $("#btnSave").data('kendoButton').enable(totalSelectedRow != 0);
    //}

    //function onChangeGrid(e) {
    //    var selectedRows = this.select();
    //    //var totalSelectedRow = selectedRows.length;
    //    var totalSelectedRow = this.selectedKeyNames().length;
    //    if (totalSelectedRow == 0) {
    //        $("#btnSave").html("Post records");
    //    } else {
    //        $("#btnSave").html("Post " + totalSelectedRow + " record(s)");
    //    }
    //    $("#btnSave").data('kendoButton').enable(totalSelectedRow != 0);
    //}

    $("#btnSave").click(function (e) {
        e.preventDefault();

        var validator = $("#form").kendoValidator().data("kendoValidator");
        if (validator.validate()) {
            var disabled = $("#form").find(':input:disabled').removeAttr('disabled');

            // Get reference to Grid object
            var grid = $("#gridSelections").data("kendoGrid");
            // Get selected rows
            var sel = $("input:checked", grid.tbody).closest("tr");
            // Get data item for each

            var selectedIds = grid.selectedKeyNames().join(",");

            $("#SelectedIds").val(selectedIds);

            $.ajax({
                type: "POST",
                url: '@Url.Action("GenerateIcs", "IcsSet")',
                data: $("#form").serialize(),
                async: true,
                beforeSend: function () {
                    window.kendo.ui.progress($(document.body), true);
                },
                success: function (result) {
                    window.kendo.ui.progress($(document.body), false);
                    if (result.Errors == "") {
                        $("#btnSave").closest(".k-window-content").data("kendoWindow").close();
                    }
                    else {
                        alert(result.Errors);
                    }
                }, error: function (req, status, errorObj) {
                    alert(req.responseText);
                }
            });
            disabled.attr('disabled', 'disabled');
        }
    });

    $("#btnCancel").click(function (e) {
        e.preventDefault();
        $("#btnCancel").closest(".k-window-content").data("kendoWindow").close();
    });
</script>







