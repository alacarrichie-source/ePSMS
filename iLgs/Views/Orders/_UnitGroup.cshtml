@(Html.Kendo().Grid<iLgs.Models.OrderItemUnitGroupVM>()
        .Name("gridItemUnitGroups_" + @ViewData["orderId"])
        .Columns(col =>
        {
            col.Bound(c => c.SetLotNo).Width(70);
            col.Bound(c => c.Qty).Width(50).HtmlAttributes(new { style = "text-align:right;" }).Format("{0:n0}")
                .ClientFooterTemplate("#=kendo.format('{0:n0}', sum)#").FooterHtmlAttributes(new { Style = "text-align:right;" })
                .HeaderHtmlAttributes(new { style = "text-align:right;" });
            col.Bound(c => c.Unit).Width(100);
            col.Bound(c => c.UnitCost).Width(50).HtmlAttributes(new { style = "text-align:right;" }).Format("{0:n2}")
                //.ClientFooterTemplate("#=kendo.format('{0:n2}', sum)#").FooterHtmlAttributes(new { Style = "text-align:right;" })
                .HeaderHtmlAttributes(new { style = "text-align:right;" }).EditorTemplateName("_UnitCostTemplate");
            col.Bound(c => c.TotalCost).Width(50).HtmlAttributes(new { style = "text-align:right;" }).Format("{0:n2}")
                .ClientFooterTemplate("#=kendo.format('{0:n2}', sum)#").FooterHtmlAttributes(new { Style = "text-align:right;" })
                .HeaderHtmlAttributes(new { style = "text-align:right;" }).EditorTemplateName("_TotalCostTemplate");
            col.Bound(c => c.InsertedDt).Width(100);
            col.Command(command =>
            {
                //command.Custom("Edit").IconClass("k-icon k-i-edit").SendDataKeys(true).Click("onClickBtnEditRisItemUnitGroup");
                command.Edit();
                //command.Destroy();
            }).Width(70);
        })
        //.ToolBar(t =>
        //{
        //    t.Create();
        //})
        .Editable(e => e.Mode(GridEditMode.InLine))
        //.Pageable(pager => pager
        //    .Input(false)
        //    .Numeric(true)
        //    .Info(true)
        //    .PreviousNext(true)
        //    .Refresh(true)
        //    .PageSizes(true)
        //    .ButtonCount(5)
        //)
        .Filterable()
        .NoRecords()
        .Resizable(r => r.Columns(true))
        .Selectable(s => s.Mode(GridSelectionMode.Single))
        .Sortable()
        .ClientDetailTemplateId("GroupDescriptions")
        .DataSource(dataSource => dataSource
            .Ajax()
            .Events(events => events.Error("GridError").RequestEnd("onGridItemUnitGroupsRequestEnd"))
            .Model(model =>
            {
                model.Id(p => p.Id);
                model.Field(p => p.Id).DefaultValue(Guid.Empty).Editable(true);
                model.Field(p => p.OrderId).DefaultValue(@ViewData["orderId"]);
                model.Field(p => p.Qty).Editable(false);
                model.Field(p => p.Unit).Editable(false);
                model.Field(p => p.InsertedDt).Editable(false);
                model.Field(p => p.TotalCost).Editable(false);
                model.Field(p => p.SetLotNo).Editable(false);
            })
            .Aggregates(a =>
            {
                a.Add(p => p.Qty).Sum();
                //a.Add(p => p.UnitCost).Sum();
                a.Add(p => p.TotalCost).Sum();
            })
            .Sort(s =>
            {
                s.Add(a => a.InsertedDt);
            })
            .Read(read => read.Action("_UnitGroupRead", "Orders", new { orderId = @ViewData["orderId"] }))
            .Update(update => update.Action("_UnitGroupUpdate", "Orders"))
        )
        .Events(events => events
            .Change("onChangeGridItemUnitGroups")
            .Edit("onEditGridItemUnitGroups")
            .DataBound("onDataBoundGridItemUnitGroups")
        )
        .AutoBind(true)
)


<script id="GroupDescriptions" type="text/kendo-tmpl">
    @(Html.Kendo().Grid<iLgs.Models.OrderItemUnitGroupDescriptionVM>()
    .Name("gridItemUnitGroupDescriptions_#=Id#")
    .Columns(col =>
    {
        col.Bound(c => c.Description).Width(100);
        col.Bound(c => c.InsertedDt).Width(100);
        //col.Command(command =>
        //{
        //    command.Edit();
        //    command.Destroy();
        //}).Width(150);
    })
    //.ToolBar(t =>
    //{
    //    t.Create();
    //})
    //.Editable(editable => editable.Mode(GridEditMode.InLine))
    .Sortable()
    .NoRecords()
    .ClientDetailTemplateId("GroupDescriptionItems")
    .DataSource(dataSource => dataSource
        .Ajax()
        .Events(events => events.Error("GridError")
        //.RequestEnd("onGridItemUnitGroupDescriptionsRequestEnd")
        )
        .Model(model =>
        {
            model.Id(p => p.Id);
            model.Field(p => p.Id).DefaultValue(Guid.Empty).Editable(false);
            //model.Field(p => p.UnitGroupId).DefaultValue("#=Id#");
            model.Field(p => p.InsertedDt).Editable(false);
        })
        .Sort(s => s.Add(a => a.InsertedDt))
        .Read(read => read.Action("_UnitGroupDescriptionRead", "Orders", new { unitGroupId = "#=Id#" }))
    )
    .Selectable(s => s.Mode(GridSelectionMode.Single))
    .Events(e => e
        .Change("onChangeGridItemUnitGroupDescriptions")
        .Edit("onEditGridItemUnitGroupDescriptions")
        .DataBound("onDataBoundGridItemUnitGroupDescriptions")
    )
    .ToClientTemplate())
</script>


<script id="GroupDescriptionItems" type="text/kendo-tmpl">
    @(Html.Kendo().Grid<iLgs.Models.OrderItemUnitGroupDescriptionItemVM>()
    .Name("gridItemUnitGroupDescriptionItems_#=Id#")
    .Columns(col =>
    {
        col.Bound(c => c.PsNo).Width(50);
        col.Bound(c => c.ItemName).Width(100);
        col.Bound(c => c.Description).Width(100);
        col.Bound(c => c.Unit).Width(30);
        col.Bound(c => c.QtyRequest).Width(30).HtmlAttributes(new { style = "text-align:right;" }).Format("{0:n0}")
                .ClientFooterTemplate("\\#=kendo.format('{0:n0}', sum)\\#").FooterHtmlAttributes(new { Style = "text-align:right;" })
                .HeaderHtmlAttributes(new { style = "text-align:right;" });
        col.Bound(c => c.PriceRate).Width(50).HtmlAttributes(new { style = "text-align:right;" }).Format("{0:n2}\\%")
                .ClientFooterTemplate("\\#=kendo.format('{0:n2}\\%', sum)\\#").FooterHtmlAttributes(new { Style = "text-align:right;" })
                .HeaderHtmlAttributes(new { style = "text-align:right;" }).EditorTemplateName("_PriceRateTemplate");
        col.Bound(c => c.UnitCost).Width(50).HtmlAttributes(new { style = "text-align:right;" }).Format("{0:n2}")
                //.ClientFooterTemplate("\\#=kendo.format('{0:n2}', sum)\\#").FooterHtmlAttributes(new { Style = "text-align:right;" })
                .HeaderHtmlAttributes(new { style = "text-align:right;" }).EditorTemplateName("_UnitCostTemplate");
        col.Bound(c => c.TotalCost).Width(50).HtmlAttributes(new { style = "text-align:right;" }).Format("{0:n2}")
                .ClientFooterTemplate("\\#=kendo.format('{0:n2}', sum)\\#").FooterHtmlAttributes(new { Style = "text-align:right;" })
                .HeaderHtmlAttributes(new { style = "text-align:right;" }).EditorTemplateName("_TotalCostTemplate");
        col.Command(command =>
        {
            command.Edit();
            command.Destroy();
        }).Width(100);
    })
    //.ToolBar(t =>
    //{
    //    t.Create();
    //})
    //.Editable(editable => editable.Mode(GridEditMode.PopUp).TemplateName("_RisUnitGroupDescriptionItemTemplate").Window(w => w.Width(1000).Title("Available Items")))
    .Sortable()
    .NoRecords()
    .DataSource(dataSource => dataSource
        .Ajax()
        .Events(events => events.Error("GridError").RequestEnd("onGridItemUnitGroupDescriptionItemsRequestEnd")
        )
        .Model(model =>
        {
            model.Id(p => p.Id);
            model.Field(p => p.Id).DefaultValue(Guid.Empty).Editable(false);
            model.Field(p => p.PsNo).Editable(false);
            model.Field(p => p.ItemName).Editable(false);
            model.Field(p => p.Description).Editable(false);
            model.Field(p => p.Unit).Editable(false);
            model.Field(p => p.QtyRequest).Editable(false);
            model.Field(p => p.TotalCost).Editable(false);
        })
        .Aggregates(a =>
        {
            a.Add(p => p.QtyRequest).Sum();
            a.Add(p => p.PriceRate).Sum();
            //a.Add(p => p.UnitCost).Sum();
            a.Add(p => p.TotalCost).Sum();
        })
        .Read(read => read.Action("_UnitGroupDescriptionItemRead", "Orders", new { unitGroupDescriptionId = "#=Id#" }))
        .Update(update => update.Action("_UnitGroupDescriptionItemUpdate", "Orders"))
        .Destroy(destroy => destroy.Action("_UnitGroupDescriptionItemDestroy", "Orders"))
    )
    .Selectable(s => s.Mode(GridSelectionMode.Single))
    .Events(e => e
        .Change("onChangeGridItemUnitGroupDescriptionItems")
        .Edit("onEditGridItemUnitGroupDescriptionItems")
        .DataBound("onDataBoundGridItemUnitGroupDescriptionItems")
    )
    .ToClientTemplate())
</script>


<script type="text/javascript">
    function onSelectIssuedItemIssuedBy(e) {
        var dataItem = this.dataItem(e.item.index());

        $("#IssuedByDesignation").val(dataItem.Desc2).trigger("change");

        //var model = $("#grid").data().kendoGrid.editable.options.model;
        //model.set('IssuedByDesignation', $('#IssuedByDesignation').val());
    }

    function onSelectIssuedItemReceivedBy(e) {
        var dataItem = this.dataItem(e.item.index());

        $("#ReceivedByDesignation").val(dataItem.Desc2).trigger("change");

        //var model = $("#grid").data().kendoGrid.editable.options.model;
        //model.set('RequestedByDesignation', $('#RequestedByDesignation').val());
    }

    function onClickGridIssuedItemAdd(subID) {
        var button = this;
        alert(subID);
        /// Get the current subgrid data item
        var subGrid = $(button).closest("[data-role=grid]").data("kendoGrid");
        var subGridItem = subGrid.dataItem(subGrid.tbody.find(".k-grid-edit-row"));

        // Get the main grid
        var mainGrid = $("#mainGrid").data("kendoGrid");

        // Select the current row in the main grid
        mainGrid.select(mainGrid.tbody.find("tr[data-uid='" + subGridItem.MainID + "']"));

        // Select the current row in the subgrid
        subGrid.select(subGrid.tbody.find("tr[data-uid='" + subGridItem.uid + "']"));

        // Get the nestedSubGrid
        var nestedSubGrid = $("#gridIssuedItems_" + subID).data("kendoGrid");

        // Add a new row to the nestedSubGrid
        nestedSubGrid.addRow();

        // Scroll the newly added row into view
        var newRow = nestedSubGrid.tbody.find("tr.k-grid-edit-row");
        if (newRow.length > 0) {
            nestedSubGrid.content.scrollTop(newRow.position().top);
        }
    }


</script>
