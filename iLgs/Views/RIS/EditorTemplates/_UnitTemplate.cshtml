@model string

@*@(Html.Kendo().ComboBoxFor(m => m)
    .Name("Unit")
    .DataTextField("Text")  // The field to display in the dropdown
    .DataValueField("Value") // The field for the underlying value
    .DataSource(source => source
        .Read(read => read.Action("GetUnits", "YourController")) // Replace with your action
    )
    .Filter("contains") // Optional filtering
    .Placeholder("Select a unit...")
)*@

@(Html.Kendo().MultiColumnComboBoxFor(m => m)
    .Name("Unit")
    .Placeholder("Select unit")
    .DataTextField("Description")
    .DataValueField("Code")
    .ValuePrimitive(true)
    .Filter(FilterType.Contains)
    .DataSource(source =>
    {
        source.Read(read =>
        {
            read.Action("GetCodes", "Getters", new { mastCode = "UNIT-GROUP", text = "" });
        })
        .ServerFiltering(true);
    })
    .Columns(c =>
    {
        c.Add().Field("Code").Title("Code").Width("20%");
        c.Add().Field("Description").Title("Description").Width("40%").Template("#= columnTemplate(data.Description) #");
        c.Add().Field("Desc2").Title("Category").Width("40%").Template("#= columnTemplate(data.Desc2) #");

    })
    .Suggest(true)
)