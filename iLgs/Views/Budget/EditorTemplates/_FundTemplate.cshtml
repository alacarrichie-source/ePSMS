@(Html.Kendo().ComboBox()
    .Name("Desc3")
    .DataTextField("Code")
    .DataValueField("Code")
    .ValuePrimitive(true)
    .Filter("startswith")
    .DataSource(source =>
    {
        source.Read(read =>
        {
            read.Action("GetCodes", "Getters", new { mastCode = "FUND", text = "" });
        })
        .ServerFiltering(false);
    })
)

